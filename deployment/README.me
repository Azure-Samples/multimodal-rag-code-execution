# Deployment to Azure
Open the deploy_public.sh bash file and look for these variables:

SUBSCRIPTION=""
RG_WEBAPP_NAME=""

Add the subscription Id and the resource group name where you want to deploy the services. 

# Prerequisites
To execute the script you should be running it as administrator
